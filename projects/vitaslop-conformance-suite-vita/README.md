# vitaslop-conformance-suite-vita

Placeholder. A generic Vita conformance corpus (SELF/ELF loading, NID import
resolution, GXM, hardware behavior). Its shape, generator, and oracle are TBD -
unlike the arm corpus, there is no simple offline oracle, so this will lean on a
Vita reference or hardware and is a later, run-only effort.

`cases/` currently holds skipped placeholders (`hello`, `cube`) carried over so
the northstar targets are not lost.
