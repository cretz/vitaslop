//! Native binary running the same wasm and wgpu stack on the desktop.
//! Implements the vitaslop-platform traits against the OS (mmap, gilrs, cpal)
//! and drives vitaslop-runtime. Built alongside the web target from day one so
//! the platform seam stays exercised.

fn main() {}
